<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Font Awesome -->
    <script src="https://kit.fontawesome.com/27a6451b4a.js" crossorigin="anonymous"></script>
    <!-- Stylesheets -->
    <link rel="stylesheet" href="css/listcss.css">
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Poiret+One&family=Sacramento&family=Abril+Fatface&family=Josefin+Sans:wght@300;700&family=Josefin+Slab:wght@400;500;600&family=Bellota+Text:wght@400;700&family=Dela+Gothic+One&family=Zen+Dots&family=Quicksand:wght@300;400;500&family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet"> 
    <!-- Script JS -->
    <script defer src="script.js"></script>
    <title>ToDo</title>
</head>
<body>
    <div class="list">
        <img class="avatar" src="images/ava-<%=imgURL%>.png" alt="">
        <h1 class="head"><%= userName%></h1>
        <h3 class="date"><%=listDate%></h3>
        <div class="todo-list">
            <div class="todo-header">
                <h2 class="list-title">TASKUP</h2>
                <span class="task-count"></span>
            </div>
            <div class="todo-body">
                <div class="tasks">
                    <%for(var i = 0; i < newListItems.length; i++) {%>
                    <div class="task">
                        <input id="task-<%= i %>" type="checkbox">
                        <label for="task-<%= i %>">
                            <span class="custom-checkbox"></span>
                            <%= newListItems[i] %>
                        </label>
                    </div>
                    <%}%>
                    <form action="/list" method="post">
                        <input type="text" name="newItem" class="new task" autocomplete="off" placeholder="Add New Task" aria-label="new task name" required>
                        <button class="btn create" name="list-add" type="submit" value="userName" aria-label="create new task">+</button>
                    </form>
                </div>
                <!-- <div class="delete-stuff">
                    <button type="submit" formaction="/clear" formmethod="post" class="btn delete completed">Clear completed tasks</button>
                    <button type="submit" formaction="/delete" formmethod="post" class="btn delete del-list" data-delete-list-button>Delete list</button>
                </div>  -->
            </div>
        </div> 
        <div class="quote-block">
            <p><%= quote %></p>
            <p class="auth">-- <%= quoteAuthor %></p>
        </div>
    </div>
</body>
</html>